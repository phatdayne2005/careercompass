HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### Tài liệu nhóm (không push lên public) ###
Agent_Information/

### Folder lạ do Maven biến chưa resolve (an toàn để ignore) ###
${project.build.directory}/

### Log files ###
*.log
logs/

### Local config override (mỗi dev có thể tạo file riêng) ###
application-local.yml
application-local.properties
application-*-local.yml
.env
.env.local

### File upload (P2 sẽ lưu transcript PDF) ###
/uploads/

### Security keys / certs (M7 OAuth có thể cần) ###
*.pem
*.key
secrets/

### Context riêng tư cho Claude Code (không chia sẻ với nhóm) ###
CLAUDE.md

# Playwright / browser test artifacts
.playwright-mcp/
/*.png
/*.jpg

# Runtime artifacts (PDF report, transcript upload)
reports/
uploads/

# Dọn repo: ảnh thiết kế / render / script thử nghiệm (giữ local, không đưa lên repo)
figmaDesign/
codex-docx-render/
scratch_script.py
remix_context.json

# Tài liệu thay đổi nội bộ (bản docx đưa cho team, không push)
CareerCompass-ThayDoi-TichHop.docx

# File bảng điểm mẫu để test transcript (không push)
bang-diem-mau.pdf

# Checklist P1 (deliverable, không push)
P1-Checklist-ChucNang.docx
P1-Checklist-va-Demo.docx
P2-Checklist-va-Demo.docx
